# Unknowns tracker

| Area | Question | Current confidence | Evidence today | Best next method | Blocking factor |
|---|---|---:|---|---|---|
| Auth | Which official login surface is most stable for automation? | Medium | Manual `my.account.sony.com/sonyacct/signin/...` URL confirmed; Safari signed-in session successfully inspected; `web.np.playstation.com/api/session/v1/session` and `/signin` behave as redirect-oriented browser endpoints | Prefer Safari summary or headed manual flow over fully headless automation | CAPTCHA / MFA / ephemeral login params |
| Web session | What browser-side markers reliably indicate signed-in state? | High | Safari inspection confirmed `isSignedIn`, `session`, `userinfo`, `pdcws2`, `pdcsi`, plus `gpdcUser` / `userId` / `chimera-*` state | Continue cross-surface browser summaries | Native clients may not use the same material |
| Tokens | What token lifetime and refresh behavior show up during official use? | Low | Browser session markers are confirmed; the PC app now clearly shows local cookie/IndexedDB/localStorage auth surfaces plus cached `grc-response.html` handoff evidence for both authorization-code and access-token-style redirects, but repo tooling intentionally redacts raw bearer/session values | Repeated redacted Safari + PC-app auth summaries over time; future bounded refresh observation | Need bounded observation plan |
| Entitlements | Where are cloud-streaming entitlements exposed, if anywhere? | Medium | Browser GraphQL/profile clues exist, current public/native PC-app assets expose `api.playstation.com/v1/users/me/lists`, `api.playstation.com/api/v2/accounts/me/attributes`, `store/api/pcnow/...`, and `store/api/chihiro/...` path families, longer sanctioned native captures show on-wire `checkout.playstation.com`, `web-commerce-anywhere.playstation.com`, `merchandise.api.playstation.com`, `commerce.api.np.km.playstation.net`, `accounts.api.playstation.com`, `client.cc.prod.gaikai.com`, and `config.cc.prod.gaikai.com`; live entitlement semantics are still unconfirmed | Compare asset strings with redacted native cache state and future metadata capture | Service surface may still be app-only or heavily gated |
| Web control plane | Which first-party browser hosts and query surfaces are active today? | High | Safari endpoint sweep confirmed `web.np.playstation.com`, `web-toolbar.playstation.com`, `io.playstation.com`, persisted GraphQL queries, Store Next.js assets, direct browser-session API replay against selected endpoints, and public JS bundles embedding query docs/config; the native PC profile remembers `web.np.playstation.com`, `static.playstation.com`, and `smetrics.aem.playstation.com`, and a longer sanctioned Windows capture now confirms on-wire `web.np.playstation.com`, `ca.account.sony.com`, `psnow.playstation.com`, `theia.dl.playstation.net`, `telemetry.api.playstation.com`, and `commerce.api.np.km.playstation.net` | Expand browser endpoint inventory when new pages are opened and compare with native cache/capture evidence | Browser-only view may still differ from native launch/stream allocation |
| GraphQL replayability | Which observed browser GraphQL operations remain directly replayable from an authenticated browser session? | Medium | Probe CLI shows a split between schema-drifted ops, access-denied ops, and direct-query-blocked attempts | Continue controlled allowlisted probe expansion and bundle correlation | Current browser/runtime context may still be incomplete |
| Session allocation | Is there still a Kamaji-named control plane in modern clients? | Medium | Current PC-app auth summary shows a `WEBDUID` cookie scoped to `/kamaji/api/pcnow/00_09_000/user`, current public PC-app assets embed `kamaji/api/psnow/00_09_000/` plus `kamaji/api/swordfish/00_09_000/`, stream-capable captures confirm on-wire `psnow.playstation.com`, `commerce.api.np.km.playstation.net`, `cc.prod.gaikai.com`, `client.cc.prod.gaikai.com`, and `config.cc.prod.gaikai.com`, segmented launch/quit slices suggest some of those are bootstrap-heavy while `client.cc` persists later in-session, and a click-from-list / waiting-for-server slice already shows active `client.cc`, `config.cc`, and Sony-owned UDP/2053 before the first visible frame; exact allocator message shapes remain unobserved | Correlate stream-phase capture timing with broker lifecycle and future shorter targeted reruns | Need cleaner segmented captures around launch/quit transitions |
| Transport | Is modern cloud streaming WebRTC-like, custom UDP, or something else? | Medium | Historical PS Now paper suggested non-WebRTC; real all-port Windows stream-phase captures now show large non-443 UDP traffic to Sony-owned `104.142.161.*:2053` and `104.142.165.*:2053` endpoints inside `104.142.128.0/17`, and the quit/save segmented slices retain the same UDP/2053 family while already in-session; that strongly suggests a custom UDP transport family, but packet semantics/codec/channel structure remain unconfirmed | Run shorter all-port captures around stream start/quit/save actions and correlate with native broker events | Need more segmented observations to separate bootstrap/control/media channels |
| Cloud saves | Do save-management actions use a distinct hostname family or ride the existing stream/control path? | Low | A copy-to-online-storage-only segmented capture kept `client.cc.prod.gaikai.com` plus Sony-owned UDP/2053 traffic active but did not surface a distinct new PlayStation save hostname family | Run a matching delete-from-online-storage-only capture and compare against the copy-only and quit-game slices | Need one more clean action-isolated save-management run |
| Video | What resolution / fps / HDR tiers are truly available by surface? | Medium | Public claims exist; Portal notes explicitly mention cloud-streaming quality options | Public-source collection + real session telemetry | Requires working official session |
| Input | Which controller features are mandatory? | Low | Community anecdotes only | Controlled user-journey notes while launching titles | Requires real client access |
| PC installer tech | What bootstrapper technology and embedded payload structure does the PC installer use? | Medium | Current installer shows WiX Burn + Advanced Installer fingerprints and embedded `PlayStationPlus.7z` / `FILES.7z` names | Deeper extraction or Windows VM install trace | Need unpacked install directory |
| PC app runtime | Is the installed official PC app still Electron/ASAR based? | High | Installed Windows payload now directly confirms `agl/resources/app.asar`, package name `playstation-now`, runtime version `9.0.4`, and a running `agl.exe --url=https://psnow.playstation.com/app/...` shell | Keep `npm run inspect:pc-app` output current when the app updates | Need newer app versions to detect drift |
| Portal | Does Portal's cloud mode expose clues via software notes or captured metadata? | Medium | Official software notes confirm feature exists | Public page collection, then local observation if hardware is available | Requires hardware for deeper validation |
| Native IPC | How much of launch/queue/stream orchestration is driven through the localhost broker? | Medium | `pspluslauncher.exe` is now directly observed listening on `ws://localhost:1235/`, the preload bridge exposes `requestGame`, `startGame`, `isStreaming`, `isQueued`, controller, audio, mic, tray, and deeplink commands, and the current public `apollo.js` still advertises Kamaji/session bootstrap config and PC-specific user/account endpoint templates | Correlate broker lifecycle with sanctioned launch/stream observations | Need a supported stream session to observe full transitions |
| Reconnect | How do sessions behave across network loss and region change? | Low | No direct evidence in repo yet | Future impairment lab after baseline session works | Requires successful baseline session |
