# Unknowns tracker

| Area | Question | Current confidence | Evidence today | Best next method | Blocking factor |
|---|---|---:|---|---|---|
| Auth | Which official login surface is most stable for automation? | **High — resolved** | Safari `prompt=none` on `ca.account.sony.com/api/authz/v3/oauth/authorize` silently redirects to `grc-response.html#access_token=...` when Safari has a prior PlayStation session; NPSSO readable via `document.cookie` AppleScript; automated Playwright blocked by Akamai on `ssocookie` endpoint (passkey is `enabled:false`, blocker is pure bot-management) | Safari silent auth is the preferred autonomous path; Playwright headed is fallback for fresh sessions | None for macOS with prior Safari session |
| Web session | What browser-side markers reliably indicate signed-in state? | High | Safari inspection confirmed `isSignedIn`, `session`, `userinfo`, `pdcws2`, `pdcsi`, plus `gpdcUser` / `userId` / `chimera-*` state | Continue cross-surface browser summaries | Native clients may not use the same material |
| Tokens | What token lifetime and refresh behavior show up during official use? | **High — confirmed** | PSNow entitlements bearer (dc523cc2): expires_in 1199s (~20 min), re-derived from NPSSO on each invocation; Chiaki Remote Play bearer (ba495a24): expires_in 3599s (~1hr) with a refresh_token for renewal; JSESSIONID: session-scoped, tied to Kamaji server lifecycle; WEBDUID: device-unique, longer-lived (path-scoped cookie) | Confirmed via live token exchanges 2026-03-30 | None outstanding |
| Entitlements | Where are cloud-streaming entitlements exposed, if anywhere? | **High — confirmed** | `GET /kamaji/api/psnow/00_09_000/user/entitlements` returns 597 live entitlements with title IDs and SKUs confirmed via authenticated access-token session (2026-03-30); endpoint requires JSESSIONID + WEBDUID + Bearer token | Live confirmed | Subscription endpoint (`/user/subscription`) still returns 404 — may have moved |
| Web control plane | Which first-party browser hosts and query surfaces are active today? | High | Safari endpoint sweep confirmed `web.np.playstation.com`, `web-toolbar.playstation.com`, `io.playstation.com`, persisted GraphQL queries, Store Next.js assets, direct browser-session API replay against selected endpoints, and public JS bundles embedding query docs/config; the native PC profile remembers `web.np.playstation.com`, `static.playstation.com`, and `smetrics.aem.playstation.com`, and a longer Windows capture now confirms on-wire `web.np.playstation.com`, `ca.account.sony.com`, `psnow.playstation.com`, `theia.dl.playstation.net`, `telemetry.api.playstation.com`, and `commerce.api.np.km.playstation.net` | Expand browser endpoint inventory when new pages are opened and compare with native cache/capture evidence | Browser-only view may still differ from native launch/stream allocation |
| GraphQL replayability | Which observed browser GraphQL operations remain directly replayable from an authenticated browser session? | Medium | Probe CLI shows a split between schema-drifted ops, access-denied ops, and direct-query-blocked attempts | Continue controlled allowlisted probe expansion and bundle correlation | Current browser/runtime context may still be incomplete |
| Session allocation | Is there still a Kamaji-named control plane in modern clients? | Medium | Current PC-app auth summary shows a `WEBDUID` cookie scoped to `/kamaji/api/pcnow/00_09_000/user`, current public PC-app assets embed `kamaji/api/psnow/00_09_000/` plus `kamaji/api/swordfish/00_09_000/`, stream-capable captures confirm on-wire `psnow.playstation.com`, `commerce.api.np.km.playstation.net`, `cc.prod.gaikai.com`, `client.cc.prod.gaikai.com`, and `config.cc.prod.gaikai.com`, segmented launch/quit slices suggest some of those are bootstrap-heavy while `client.cc` persists later in-session, and a click-from-list / waiting-for-server slice already shows active `client.cc`, `config.cc`, and Sony-owned UDP/2053 before the first visible frame; new standalone Gaikai probing confirms `POST https://config.cc.prod.gaikai.com/v1/config` (200), `GET/POST https://cc.prod.gaikai.com/v1/apollo/id` (200 → `clientSessionId` / `apolloId`), `POST https://client.cc.prod.gaikai.com/v1/events|logs` (200 with `X-Gaikai-ClientSessionId`), and the CLI now exposes that whole surface via `gaikai id|config|auth-code|event|log|preflight`, but exact allocator message shapes remain unobserved because final launch is still mediated by native `requestGame` / `startGame` plugin calls | Correlate stream-phase capture timing with broker lifecycle and use the new broker send/replay path for `requestClientId`, `setSettings`, `setAuthCodes`, `requestGame`, `startGame` | Need the native plugin/broker path, especially `streamServerClientId`, to cross the final launch boundary |
| Transport | Is modern cloud streaming WebRTC-like, custom UDP, or something else? | Medium | Historical PS Now paper suggested non-WebRTC; real all-port Windows stream-phase captures now show large non-443 UDP traffic to Sony-owned `104.142.161.*:2053` and `104.142.165.*:2053` endpoints inside `104.142.128.0/17`, the quit/save segmented slices retain the same UDP/2053 family while already in-session, and an overlay-only capture keeps that UDP family alive without surfacing fresh PlayStation DNS/TLS hostnames; that strongly suggests a custom UDP transport family, but packet semantics/codec/channel structure remain unconfirmed | Run shorter all-port captures around stream start/quit with explicit action timing and correlate with native broker events | Need more segmented observations to separate bootstrap/control/media channels |
| Cloud saves | Do save-management actions use a distinct hostname family or ride the existing stream/control path? | Low | A copy-to-online-storage-only segmented capture kept `client.cc.prod.gaikai.com` plus Sony-owned UDP/2053 traffic active but did not surface a distinct new PlayStation save hostname family | Optional: run a matching delete-from-online-storage-only capture if save-management semantics become important again | Low priority unless save behavior becomes an MVP requirement |
| Video | What resolution / fps / HDR tiers are truly available by surface? | Medium | Public claims exist; Portal notes explicitly mention cloud-streaming quality options | Public-source collection + real session telemetry | Requires working official session |
| Input | Which controller features are mandatory? | Low | Community anecdotes only | Controlled user-journey notes while launching titles | Requires real client access |
| PC installer tech | What bootstrapper technology and embedded payload structure does the PC installer use? | Medium | Current installer shows WiX Burn + Advanced Installer fingerprints and embedded `PlayStationPlus.7z` / `FILES.7z` names | Deeper extraction or Windows VM install trace | Need unpacked install directory |
| PC app runtime | Is the installed official PC app still Electron/ASAR based? | High | Installed Windows payload now directly confirms `agl/resources/app.asar`, package name `playstation-now`, runtime version `9.0.4`, and a running `agl.exe --url=https://psnow.playstation.com/app/...` shell | Keep `npm run inspect:pc-app` output current when the app updates | Need newer app versions to detect drift |
| Portal | Does Portal's cloud mode expose clues via software notes or captured metadata? | Medium | Official software notes confirm feature exists | Public page collection, then local observation if hardware is available | Requires hardware for deeper validation |
| Native IPC | How much of launch/queue/stream orchestration is driven through the localhost broker? | Medium | `pspluslauncher.exe` is now directly observed listening on `ws://localhost:1235/`, the preload bridge exposes `requestGame`, `startGame`, `isStreaming`, `isQueued`, controller, audio, mic, tray, and deeplink commands, the current public `apollo.js` advertises the concrete launch sequence `setSettings -> requestClientId -> setTitleInfo -> getCloudAuthCode -> getPS3AuthCode -> getStreamServerAuthCode -> setAuthCodes -> requestGame -> startGame`, standalone probing now confirms the HTTP-side preflight pieces (Gaikai client session + config + events/logs + `gaikai://local` auth codes), and `psn-direct-cli.ts` now has `broker send` for raw/structured replay; newer bundle-level evidence narrows the expected contract further: PC `requestClientId()` takes no params and should surface `gkClientId`, `ps3GKClientID`, and `streamServerClientId`; PC `setSettings()` appears to take an object payload; PC auth-code staging may be folded back into `setSettings` via `gkCloudAuthCode`, `gkPs3AuthCode`, and `streamServerAuthCode`; and PC `requestGame()` appears to reduce to a boolean `forceLogout` call after state staging. A local mock broker emulator now exists for app-free envelope iteration and logging, and a macOS shell harness can now run Sony's real public bundle against that local broker to exercise both the **BrowserAPI** path and the require-able **PCClientAPI** path. The Mac PC harness now directly confirms PC-side `WINDOWS` / `PC` settings, `requestClientId`, `getStreamServerAuthCode()`, auth staging folded into later `setSettings`, `requestGame`, escalation to a broker `startGame` call once a synthetic `GotLaunchSpec` is emitted, and post-launch controller-routing commands (`routeInputToClient`, `routeInputToPlayer`). However, the saved screenshot still shows only the Plus shell/splash UI and not a game frame, which strongly suggests the real pixels still live beyond the browser-visible broker boundary in the native player/runtime. The remaining blocker is therefore still the **real** PC-native reply/runtime path and actual media behavior | Use the macOS shell harness for continued BrowserAPI/PCClientAPI capture, especially around post-`requestGame` synthetic event sequencing and post-`startGame` routing/input commands, and use `broker send` against a live running app to capture the true PC-native `streamServerClientId` / `startGame` contract and compare it with the Mac-side traces | Need a running native app/plugin instance to execute the real PC-native commands and surface real media/runtime behavior |
| Reconnect | How do sessions behave across network loss and region change? | Low | No direct evidence in repo yet | Future impairment lab after baseline session works | Requires successful baseline session |
