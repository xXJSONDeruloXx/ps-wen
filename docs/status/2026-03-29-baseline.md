# Baseline status — 2026-03-29

## Completed in repo setup phase

- initialized repo scaffold for research, testing, and artifacts
- added `.env` handling and ignored local secrets
- documented guardrails and clean-room assumptions
- created structured research docs from the seed dossier
- added public-source, static-analysis, and network-capture workstreams

## Immediate next actions

1. Create and push the GitHub repo.
2. Install npm dependencies and verify the TypeScript / Playwright toolchain.
3. Run `npm run research:public` to collect fresh first-party page snapshots.
4. Fix any broken source URLs discovered by the first pass.
5. Fill `.env` when ready and attempt the official-login smoke harness.
6. If an official PC client bundle is available locally, run the static inventory helper.

## Biggest blockers

- Current official URL stability is unverified until the first fetch run.
- A fully autonomous sign-in path may be limited by CAPTCHA or MFA.
- Modern cloud streaming may only expose crucial session behavior from non-browser clients.

## Decision for the next commit series

Proceed by maximizing reproducible evidence collection first, then use those artifacts to tighten the implementation plan.
